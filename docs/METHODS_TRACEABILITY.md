# Methods Traceability

This map is for reviewers who want to connect scientific claims to source code,
tests, and generated artifacts.

| Scientific component | Code authority | Verification | Review artifacts |
| --- | --- | --- | --- |
| Corrected Conway-input agreement and LoA calculations (`agreement_natural_log_tau2_direct_v1`) | `src/tcco2_accuracy/core/conway_meta.py`, `src/tcco2_accuracy/core/bland_altman.py` | Independent equation-oracle, scale-equivalence, boundary, and legacy-fixture tests in `tests/core/` | `artifacts/meta_loa_check.md`, `artifacts/manuscript_parameters.*`, `artifacts/conway_table1_fixture_summary.md` (legacy comparator) |
| Bootstrap uncertainty for bias, within-study variance, and heterogeneity | `src/tcco2_accuracy/core/bootstrap.py` | `tests/core/test_bootstrap.py`, `tests/core/test_hybrid_bootstrap_and_conditional.py` | `artifacts/bootstrap_params.csv`, `artifacts/bootstrap_summary.md` |
| PaCO2 subgroup assignment and private prior building | `src/tcco2_accuracy/core/paco2.py`, `src/tcco2_accuracy/data.py` | Synthetic fixtures in `tests/core/test_paco2_distribution.py`, `tests/core/test_paco2_prior_bins.py`; release-contract checks in `tests/contracts/test_data_governance.py` | No restricted-derived PaCO2 distribution or prior is tracked/staged |
| Forward TcCO2 classification performance | `src/tcco2_accuracy/core/simulation.py` | `tests/core/test_simulation.py`, `tests/workflows/test_workflows.py` | `artifacts/simulation_summary.md` retained frozen; Table 1 outputs removed |
| Inverse TcCO2 to PaCO2 inference | `src/tcco2_accuracy/core/inference.py`, `src/tcco2_accuracy/ui_api.py` | `tests/core/test_inference.py`, `tests/contracts/test_ui_api.py` | `artifacts/manuscript_table3_prediction_intervals.*` retained frozen; inference demo removed |
| Conditional misclassification and two-stage strategy | `src/tcco2_accuracy/core/conditional.py`, `src/tcco2_accuracy/core/two_stage.py` | `tests/core/test_two_stage.py`, `tests/core/test_hybrid_bootstrap_and_conditional.py` | `artifacts/manuscript_table2_two_stage.md` and `artifacts/two_stage_summary.md` retained frozen; detailed CSV/conditional outputs removed |
| Static browser app contract and plotting | `src/tcco2_accuracy/browser_contract.py`, `web/assets/js/app.js`, `web/pyodide_worker.js` | `tests/contracts/test_browser_contract.py`, `tests/contracts/test_stage_web_python.py`, `tests/e2e/test_web_app.py` | GitHub Pages app and staged assets generated by `make stage-web` |

`make verify` is the repository-level gate. Manuscript/reviewer artifacts should
be rebuilt to a scratch directory and compared with a baseline whenever core,
workflow, or reporting code changes.

Only the artifacts labeled `corrected-provisional` in `artifacts/STATUS.md` are current for
the corrected agreement method. PaCO2-dependent manuscript, simulation, inference, and
classification artifacts remain frozen at the legacy method until the downstream regeneration,
governance, and independent-review gates are complete.

`docs/data_release_contract.json` is the machine-readable current-tree authority. Normalized
restricted-derived weights are governed like exact counts because they may reconstruct the source
distribution. Browser default inference is likelihood-only; prior-weighted inference is upload-only
and client-side. Removed current-tree files may remain in prior commits, tags, clones, caches, or
historical deployments because this wave does not rewrite history.
